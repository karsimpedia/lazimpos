version: "3.9"
services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: pos
    ports: ["5432:5432"]
    volumes: [ "pg:/var/lib/postgresql/data" ]

  redis:
    image: redis:7
    ports: ["6379:6379"]

volumes:
  pg:
